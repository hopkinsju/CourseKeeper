<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="CourseKeeper.Views.CourseDetailPage"
			 xmlns:sys="clr-namespace:System;assembly=mscorlib"
             Title="{Binding Name}">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Edit" Clicked="Edit_Clicked" />
        <ToolbarItem Text="Delete" Clicked="Delete_Clicked" />
    </ContentPage.ToolbarItems>
	<ContentPage.Content>
		<ScrollView>
        <StackLayout Spacing="10" Padding="10" VerticalOptions="FillAndExpand">
		<StackLayout VerticalOptions="FillAndExpand">
		<Label XAlign="Center" Text="{Binding StartDate, StringFormat='Start Date: {0}'}" />
		<Label XAlign="Center" Text="{Binding EndDate, StringFormat='End Date: {0}'}"/>
		</StackLayout>
            			<Label Text="Send Notifications Before Course End?"/>
			<Switch x:Name="CourseNotificationToggle" IsToggled="{Binding Notifications}" IsEnabled="false"/>
        </StackLayout>
			</ScrollView>
    </ContentPage.Content>
</ContentPage>

